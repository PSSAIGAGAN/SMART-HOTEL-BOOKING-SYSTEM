# src/main/resources/bootstrap.properties for hotel-service
spring.application.name=hotel-service
spring.cloud.config.uri=http://localhost:8000 # Use spring.cloud.config.uri for config server location
spring.config.import=optional:configserver:${spring.cloud.config.uri} # Import from the config server